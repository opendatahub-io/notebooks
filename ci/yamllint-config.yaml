---

extends: default

rules:
  line-length: disable
  new-line-at-end-of-file:
    level: warning

  document-start:
    ignore:
      # generated file
      - 'pnpm-lock.yaml'
      # synced from opendatahub-io/security-config, protected by org push ruleset
      - '.coderabbit.yaml'
      - 'semgrep.yaml'
