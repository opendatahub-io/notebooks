# PyTorch + LLM Compressor runtime

Elyra pipeline runtime for the llmcompressor workbench (`jupyter/pytorch+llmcompressor/`).

Dependency alignment, RHAIIS 3.2.2 source-of-truth links, and lockfile regeneration instructions are documented in [jupyter/pytorch+llmcompressor/README.md](../../jupyter/pytorch+llmcompressor/README.md). Keep `ubi9-python-3.12/pyproject.toml` constraint blocks in sync between workbench and runtime.
