class QuayCommand:
    """Commands to work with Quay.io images, including getting latest images, URLs, commit hashes, etc"""

    def execute(self, args):
        print(f"Quay command executed with: {args}")
